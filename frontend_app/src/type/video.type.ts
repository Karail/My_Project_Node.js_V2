
export type itemsVideoType = {
    id: number,
    name: string,
    url: string,
    fileName: string,
    user_id: number | null,
    views: number | null,
    like: number | null,
    dislike: number | null,
    preview: string,
    createdAt: string,
    updatedAt: string,
}