
export type itemsVideoType = {
    id: number,
    name: string,
    url: string,
    fileName: string,
    user_id: number | null,
    views: number,
    like: number | null,
    dislike: number | null,
    preview: string,
    createdAt: string,
    updatedAt: string,
}